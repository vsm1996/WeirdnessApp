import React from "react";
import PropTypes from "prop-types";

const Outcome = props => {
  return (
    <div className="p1">
      <p>Outcome</p>
    </div>
  );
};

Outcome.propTypes = {};

export default Outcome;
